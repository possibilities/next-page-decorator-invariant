import PageDecoratorInvariant from 'next-page-decorator-invariant'

// Normally we'd put this with other global decorators but for the demo
// we simply pass it through.
const pageDecoratorInvariant = PageDecoratorInvariant('Page')

export default pageDecoratorInvariant
